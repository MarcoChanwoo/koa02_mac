let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_19c‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_19c‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_19c‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_19c‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_19c‍.w("./api",[["default",["api"],function(v){api=v}]]);_19c‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();
// const Koa = require('koa');
// const Router = require('koa-router');
// const bodyParser = require('koa-bodyparser');
// const mongoose = require('mongoose');







// import createFakeData from './createFakeData';

// 비구조화 할당을 통해 process.env 내부 값에 대한 레퍼런스 제작
const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI)
  .then(() => {
    console.log('Connected to MongoDB');
    // createFakeData();
  })
  .catch((e) => {
    _19c‍.g.console.error(e);
  });

// const api = require('./api');

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes());

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware());

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

// app.listen(4000, () => {
//   console.log('Listening to port 4000');
// });

// PORT가 지정되어 있지 않다면 4000사용
const port = PORT || 4000;
app.listen(port, () => {
  _19c‍.g.console.log('Listening to port %d', port);
});
